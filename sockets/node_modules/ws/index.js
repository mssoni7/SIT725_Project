const websocket = require ("ws");

const wss = websocket.Server({ port: 5500 });

wss.on("connection", ws => {
    console.log("A New Client Has Joined");

    ws.on("close", () => {
        console.log("Client has disconnected");
    });
});
